---
import { buildSrc } from '@imagekit/javascript';
import { getImageKitConfig } from '../lib/imagekit';
import { OG_IMAGE_WIDTH, OG_IMAGE_HEIGHT } from '../constants/sizes';
import type { OgImageProps } from '../types/index';

interface Props extends OgImageProps {}

const {
  src,
  alt,
  twitterTitle,
  urlEndpoint,
  transformation = [],
  queryParameters,
  transformationPosition,
  format,
  ...restProps
}: Props = Astro.props;

// Validate required props
if (!twitterTitle) {
  throw new Error('[OgImage] twitterTitle is required for the OgImage component.');
}

// Resolve config
const config = getImageKitConfig({ urlEndpoint, transformationPosition });

// Determine dimensions
const width: number = restProps.width ?? OG_IMAGE_WIDTH;
const height: number = restProps.height ?? OG_IMAGE_HEIGHT;

// Generate OG image URL (default format, typically jpg)
const ogImageUrl = buildSrc({
  src,
  urlEndpoint: config.urlEndpoint,
  transformation: [
    ...transformation,
    { width, height },
  ],
  queryParameters,
  transformationPosition: config.transformationPosition,
});

// Generate Twitter image URL (WebP for better compression where supported)
const twitterImageUrl = buildSrc({
  src,
  urlEndpoint: config.urlEndpoint,
  transformation: [
    ...transformation,
    { width, height, format: format || 'webp' },
  ],
  queryParameters,
  transformationPosition: config.transformationPosition,
});
---

<meta property="og:title" content={twitterTitle} />
<meta property="og:image" content={ogImageUrl} />
<meta property="og:image:secure_url" content={ogImageUrl} />
<meta property="og:image:width" content={String(width)} />
<meta property="og:image:height" content={String(height)} />
{alt && <meta property="og:image:alt" content={alt} />}
<meta property="twitter:title" content={twitterTitle} />
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:image" content={twitterImageUrl} />
